const React = require("react");
const { shallow, mount } = require("enzyme");
const App = require("../components/App.tsx");

describe("Tests", () => {
  it("should run tests", () => {
    expect(true).toEqual(true);
  });
});
